##
Задача: 
    найти в int массиве заданное число и вернуть его индекс (позицию в массиве).
    Если такого числа не нашлось, вернуть последний индекс массива.
    (Последний индекс = длина - 1)
##

let find arr num =
    let helper arr num current len => if( 
        <=(len, current) 
        if ( 
            ==(get(arr, current), num) ## Встроенная функция get какая-нибудь ##
            num
            helper(
                arr 
                num 
                +(current 1)
                len
            )
        )
        -(len 1)
    )
=> helper(
    arr
    num
    0
    len(arr) ## Встроенная функция len какая-нибудь ##
)

find ([1 2 3 4 5 6 7 8 9] 9)